# GitHub Bookshop

A small Three.js prototype that renders a 3D bookshop where each book represents a GitHub repository.

## Run

```bash
npm install
npm run dev
```

## Notes

- The app fetches pseudo-random repositories from the GitHub Search API.
- Clicking a book shows repository details in the side panel.
- GitHub API rate limits can affect loading if you refresh repeatedly.
